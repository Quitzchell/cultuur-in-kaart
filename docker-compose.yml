
services:
    development:
        build:
            context: .
            dockerfile: Dockerfile.dev
        volumes:
            - .:/var/www/html:delegated
        ports:
            - "8080:80"
    
    # Staging service
    staging:
        build:
            context: .
            dockerfile: Dockerfile.staging
        ports:
            - "8080:80"
